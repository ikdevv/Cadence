# Used by docker-compose.yml for the postgres service and to build each
# service's DATABASE_URL. Copy to .env and adjust for non-local environments.
POSTGRES_USER=cadence
POSTGRES_PASSWORD=cadence
POSTGRES_DB=cadence
