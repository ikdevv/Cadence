DATABASE_URL="postgresql://cadence:cadence@localhost:5432/cadence?schema=public"
PORT=3001

JWT_ACCESS_SECRET="replace-with-a-long-random-string"
JWT_REFRESH_SECRET="replace-with-a-different-long-random-string"
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d

CORS_ORIGIN=http://localhost:3000
INVITATION_TTL_HOURS=48
<<<<<<< Updated upstream
=======

# SMTP — Mailpit in local dev (docker-compose service `mailpit`, no auth
# needed; inbox UI at http://localhost:8025). Point at a real provider's SMTP
# endpoint in other environments.
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM="Cadence <no-reply@cadence.local>"
# SMTP_SECURE=true
# SMTP_USER=
# SMTP_PASS=

# AI Report Assistant — server-side only, never exposed to the browser.
# The token needs the "Make calls to Inference Providers" permission, and the
# model must be served by a provider enabled on your account — check yours with
# GET https://router.huggingface.co/v1/models.
HUGGINGFACE_API_KEY=
HUGGINGFACE_MODEL=Qwen/Qwen3-Next-80B-A3B-Instruct
>>>>>>> Stashed changes
